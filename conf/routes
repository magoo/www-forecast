# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       Auth.Index
GET     /list                                   List.Index

GET     /logout                                 Logout.Index

GET     /create                                 Create.Index
POST    /create                                 Create.Create
GET     /view/:sid                              View.Index
POST    /view/:sid/edit                         View.Update
GET     /view/:sid/results                      Results.Index
POST    /view/:sid/conclude                     View.Conclude
POST    /view/:sid/record                       View.AddRecord

GET     /estimate                               Estimate.Index
POST    /estimate                               Estimate.Create
GET     /view/estimate/:eid                     ViewEstimate.Index
GET     /view/estimate/:eid/results             estimateresults.Index
POST    /view/estimate/:eid/conclude            ViewEstimate.Conclude
POST    /view/estimate/:eid/edit                Estimate.Update

GET     /rank                                   Rank.Index
POST    /rank                                   Rank.Create
GET     /view/rank/:rid                         ViewRank.Index
GET     /view/rank/:rid/results                 RankResults.Index
POST    /view/rank/:rid/edit                    Rank.Update
#POST    /view/rank/:rid/conclude               ViewRank.Index

# GET     /auth                                   Auth.Index
POST    /auth                                   Auth.GoogleToken

POST    /forecast                               Forecast.Create

POST    /range                                  Range.Create

POST    /delete                                 Delete.DeleteScenario
POST    /delete/estimate                        Delete.DeleteEstimate
POST    /delete/ranks                           Delete.DeleteRank

POST    /sort                                   Sort.Create

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action
